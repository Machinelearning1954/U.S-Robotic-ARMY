# Source

Python source code for the data pipelines and models.

Suggested modules as the project grows:

- `data/` — data collection, loading, and preprocessing
- `models/` — model definitions and training loops
- `evaluation/` — metrics and evaluation scripts
- `utils/` — shared helpers
